/**
* define project constants
*/
// export const catchDownloadDirectory = `${process.env[process.platform === 'darwin' ? 'HOME' : 'USERPROFILE']}/.fino-template/`
export const noteCacheDirectory = `${process.env[process.platform === 'darwin' ? 'HOME' : 'USERPROFILE']}/Desktop/.waveatom-note/`
